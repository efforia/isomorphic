import {} from '../actions/hardware'

const initialState = {
  inCreation: {},
  current: {}
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
